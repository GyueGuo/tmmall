<wxs module="util" src="../../utils/util.wxs" />
<image class='logo' src='{{configSwitch.appInfo.logo}}'></image>
<view class='head'>{{configSwitch.appInfo.title}}申请获得以下权限:</view>
<view class='subhead'>获得你的公开信息(昵称,头像等)</view>

<button wx:if="{{ canIUseGetUserProfile }}" class='submit' loading='{{loading}}' disabled='{{disabled}}' bindtap="handleGetUserProfile" style='{{util.diyColor("",diyColor.zColor,"")}}'>
  确认授权
</button>
<button wx:else class='submit'loading='{{loading}}' disabled='{{disabled}}' open-type='getUserInfo' bindgetuserinfo="handleGetUserInfo" style='{{util.diyColor("",diyColor.zColor,"")}}'>
  确认授权
</button>